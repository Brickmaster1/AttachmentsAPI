{
  "schemaVersion": 1,
  "id": "${mod_id}-tests",
  "version": "${version}",
  "name": "${mod_name} Tests",
  "contact": {
    "sources": "${mod_repository}"
  },
  "license": "${mod_license}",
  "icon": "assets/${mod_id}-test/icon.png",
  "environment": "*",
  "entrypoints": {
    "fabric-gametest": ["${group}.${mod_id}.gametest.FabricGameTest"],
    "attachmentsapi:attachments": [
      "${group}.${mod_id}.gametest.TestAttachments"
    ]
  }
}